/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Runtime config (e.g. the Google client id) is fetched from GET /api/config,
// not baked in as a VITE_ build-time var — so no custom ImportMetaEnv here.
